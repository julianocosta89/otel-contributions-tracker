# OTel Contributions Tracker

A single-file static web app that visualizes [OpenTelemetry contribution data](https://insights.linuxfoundation.org/project/opentelemetry/contributors) sourced from the Linux Foundation Insights API. Designed to run on GitHub Pages with zero build tooling.

## Features

- **Overview** — stat cards for total contributors, organizations, maintainers, and reviewers (with % change vs. prior period), a contribution concentration chart, top organizations, and geographic reach
- **Contributors** — full leaderboard with avatars, GitHub handles, contribution counts, percentage share, and role badges; paginated 25 per page with search by name or handle
- **Organizations** — horizontal bar chart of the top 15 organizations plus a paginated leaderboard table
- **Concentration** — bus factor analysis showing how many top contributors account for what percentage of all work, with per-contributor and per-organization breakdowns
- **Geography** — country-level bar chart and table with flags, contribution counts, and share bars
- **Global filters** — date presets (30d, 90d, 6m, 1y), custom date range, platform filter, and activity type filter

## Project Structure

```
index.html                          # entire web app (HTML + vanilla JS + Tailwind/Chart.js via CDN)
docs/
  README.md                         # docs directory guide
  concentration.html                # standalone page explaining the HHI concentration metric
scripts/
  README.md                         # scripts directory guide
  fetch-data.mjs                    # fetches LF Insights API → data/cache.json (daily)
  fetch-sigs.mjs                    # fetches per-repo SIG data → data/sigs.json (daily)
  fetch-affiliations.mjs            # builds handle→company map from CNCF gitdm → data/affiliations.json (weekly)
  fetch-github-companies.mjs        # fills affiliation gaps via GitHub API → data/github-companies.json (weekly)
  fetch-roles.mjs                   # fetches GitHub team roles → data/roles.json (weekly)
data/
  README.md                         # data directory guide
  cache.json                        # main data cache (generated by fetch-data.mjs)
  sigs.json                         # per-repo SIG leaderboards (generated by fetch-sigs.mjs)
  affiliations.json                 # CNCF gitdm handle→company map (highest-confidence affiliation source)
  github-companies.json             # GitHub profile company field (fallback affiliation source)
  roles.json                        # GitHub team role assignments
.github/
  workflows/
    refresh-data.yml                # daily: fetch-data.mjs + fetch-sigs.mjs
    refresh-affiliations.yml        # weekly: fetch-affiliations.mjs
    refresh-roles.yml               # weekly: fetch-roles.mjs
    refresh-sigs.yml                # weekly: fetch-sigs.mjs (full refresh)
```

### `index.html`

The entire web application lives in a single file — no build step, no framework, no bundler. HTML structure, vanilla JavaScript, and CDN-loaded Tailwind CSS and Chart.js are all inline. All UI changes go here.

### `docs/`

Standalone HTML pages that provide supplementary explanations for metrics and features used in the app. Each page is self-contained (same Tailwind CDN stack, dark theme to match the app) and linked from `index.html` at the point where users need more context than a tooltip can offer. See [`docs/README.md`](docs/README.md) for the page inventory and conventions for adding new pages.

### `scripts/`

Node.js 20+ ESM scripts that populate the `data/` directory. No `npm install` is needed — the scripts use only built-in Node.js modules and fetch. Run `fetch-data.mjs` and `fetch-affiliations.mjs` before `fetch-github-companies.mjs`, since the latter fills gaps left by the former two. See [`scripts/README.md`](scripts/README.md) for per-script options and the recommended run order.

### `data/`

All files in this directory are generated by the scripts above, committed to the repository, and loaded by the app at startup. Committing generated data means the web app runs without any server-side logic — it reads static JSON files directly. The GitHub Actions workflow keeps these files up to date automatically. See [`data/README.md`](data/README.md) for a description of each file's schema.

## Getting Started

**Prerequisites:** Node.js 20+ (only needed to generate the cache; the web app itself has no dependencies).

1. Fork or clone the repository.

2. Fetch and cache the data:

   ```bash
   node scripts/fetch-data.mjs
   ```

   This creates `data/cache.json` by paginating all relevant LF Insights API endpoints.

3. Commit the generated cache file and push:

   ```bash
   git add data/cache.json
   git commit -m "chore: seed initial data cache"
   git push
   ```

4. Enable GitHub Pages: go to **Settings → Pages** and set the source to deploy from branch `main`, root directory.

5. The included GitHub Actions workflow refreshes `data/cache.json` daily at 06:00 UTC and commits the update automatically. No further configuration is required.

## How It Works

### Data caching

The app loads `data/cache.json` at startup. When the active view matches the default settings (1-year window, all platforms, all activity types), the cached dataset is used directly — no network requests, no CORS issues, and search covers the full dataset.

Changing any filter triggers live API calls to the LF Insights endpoint. A badge in the app header indicates whether data is coming from the cache (with its date) or from a live request.

### Tech stack

- Pure HTML and vanilla JavaScript — no build step, no `npm install`
- [Tailwind CSS](https://tailwindcss.com/) via CDN
- [Chart.js](https://www.chartjs.org/) via CDN

## Contributing

Contributions are welcome. Because the app is a single HTML file, all UI changes happen in `index.html`. For data pipeline changes, edit the relevant script in `scripts/` — each script is self-contained and documented in [`scripts/README.md`](scripts/README.md). Open a pull request with a clear description of what the change does and why.
